# SyncSocial Web Console（Next.js）

Phase 0 目标：登录页 + 左侧导航（用户功能/管理员管理）+ 管理员用户管理。

本地运行（示例）：
1) 安装依赖：`npm install`
2) 配置环境变量：复制 `.env.example` → `.env`
3) 启动开发服务器：`npm run dev`

默认访问：`http://localhost:3000`
